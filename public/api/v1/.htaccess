<FilesMatch ".*">
    SetHandler cgi-script
</FilesMatch>

Options +ExecCGI
AddHandler cgi-script .cgi

# Allow numbers only in dynamic path segments!

RewriteEngine On
RewriteRule "^courses$"  "courses?id=all" [L]
RewriteRule "^courses/(\d+)$"  "courses?id=$1" [L]
RewriteRule ".*"  "404" [L]
